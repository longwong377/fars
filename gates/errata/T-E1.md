# Erratum: T-E1 superseded by T-E1d

## Measurement
T-E1 used a "face JND" with no defined distance, so it could not be measured. T-E1d defines the distance (population-SD-scaled face and body parameters that change the drawn head) and d* (the psychometric 50 % point of blind "same person" judgements on rendered crops at 1 m and 5 m).

The new row is at least as strict as T-E1 on everything it does not correct. T-E1 stays in gates/thresholds.json, reads
SUPERSEDED on the board and is not evaluated once this erratum is signed.

## Signature
Proposed by the second independent critique of the plan, REVIEWS/master_plan_critique_rev2.md §3.3 (session 8). To be
countersigned by the critic of the next audit round (MASTER_PLAN T-R2), who adds a line here naming their review file; until
then both rows are evaluated.
