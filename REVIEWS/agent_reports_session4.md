# Agent reports, session 4 (the ones whose agents could not write a report file)

Copied by the lead from the agents' final messages. The agents' decision entries in DECISIONS.md carry the same content:
D-150 (sim), D-155 (faces), D-156 (atmosphere), D-157 (surfaces). The crowd agent's report is `REVIEWS/agent_crowd_scale.md`
**inside its bundle** (`handoff/branches/crowd_D143.bundle`: not merged, see HANDOFF.md); the plain look is
`REVIEWS/agent_plain_look.md`; the performances `REVIEWS/agent_performances.md`; carving: D-151.

## Surfaces (D-157) — merged
**Still weak or unverified**
1. **The hall-floor sheen fix is unrendered.** Run 1 showed the polished red floors of the Apadana and Hadish halls as a flat blue-white sheen: sky reflection leaking indoors (the probes' low-order visibility averages over a wide cone; at grazing angles a floor reflects ~40 %). Apadana floor R/G (linear) 2.95 → 0.94, ~8× brighter; Hadish dark-red speckle. `ce6c48e` adds Frostbite (Lagarde & de Rousiers) specular occlusion: on the CPU the occlusion is 0.0000 on the Apadana floor and 0–0.007 on the Hadish floor 3–10 m out; no frame shows it yet.
2. **Direct-light GI inside the volumes (the lead's item 6) is not measured where it matters.** Run 1 timed out before the scribes' room and the outdoor view. In the Apadana hall it adds 3.5 % to the frame mean, mostly GI noise along the flutes; faint GI speckle on the scribes' room's dark wall.
3. **Draw calls and triangles at apadana-north-nr and stair-foot-east unmeasured.** apadana-hall-in: 530 draws with and without; triangles 10,038,250 → 10,115,416 with bevels (+0.8 %). Portico: 544 draws; 10,149,952 → 10,228,026.
- Not done: PCSS (CSM takes no custom shadow filter: needs a patch to three's CSMShadowNode); dust under porticoes (a kept palace was swept, §7); stair merlons not bevelled (`decor.ts`, `crenellationGeometry` has `bevelEnabled`); Terrace platform prisms not bevelled; no wall-foot band where terrain (not a part) meets the wall; the orange probe blotches come from the 2 m probe spacing (local contrast 5–6.5×), present with all surface changes off.

**Changed (all C):** broad colour variation at 6/1.5/0.4 m, CPU-calibrated 1σ: plaster 8.8 %, mud plaster 9.7 %, dressed stone 6.2 %, red floor 5.9 % (was 1–2 %). Ashlar courses 0.8–1.3 m, blocks 1.15–3.45 m, ±13 % tone with a warm/cool split, faces tilted ≤ ±0.43°, a 5 mm worn edge beside the 0.8 mm joint (Q-071 kept). Per-vertex attributes from `meshes.ts` (parts unchanged): splash band at wall feet, traffic wear on hall floors, run-off under exposed stone tops. Bevels on render geometry only, on free edges (5,438 of 26,616): architecture 27,492 → 44,734 triangles (≤ 0.7 % of the 12 M budget with shadows). A 64 px sky/ground environment map for specular only (roughness < 0.7) with the occlusion above; SSR at high/ultra (roughness < 0.5, 30 m, 0.3 m thick, half resolution): the Hadish red floor reflects the doorway; polished dark frames reflect the sky (+0.10 linear). GI from direct light only, added inside the probe volumes too. GI thickness 0.25 m (linear with distance) plus contact samples within 1.2 m; sun contact shadows that darken only a pixel's share of direct sun. Frame time about unchanged (20–23 s vs 21–25 s per frame, SwiftShader 960×540).
**Tools:** `tests/e2e/dbg_surf.spec.ts` (A/B variants in one load), `tests/shader_build.test.ts` (builds the shaders in node), `tests/lib/occ_check.ts`, `regions.py`, `probe_slice.ts`. Q-260–Q-264.
**Next render it asked for:** apadana-hall-in B+env0, hadish-hall B+env0, scribe-at-work B+gi0, apadana-enter B+gi0, stair-foot-east and apadana-north-nr at 2 frames with B+A.

## Atmosphere (D-156) — merged; **never compiled in a browser**
**Still wrong or unverified**
1. **Nothing has been seen in a browser.** The horizon colour nodes, the aerial fog node, the new cloud march and the hall light-shaft pass have never been compiled on a GPU backend. A compile failure in the colour nodes or the fog node would break every lit material at every quality. Bisect switches: `?air=0` (drops the colour nodes and the fog node; main.ts's FogExp2 then draws at its fixed density) and `?airlight=0` (drops the shaft pass). Renders to make first: `dawn-sunrise`/`stair-dawn-plain` (in shadow out to ~9.75 km at 05:51; `tools/dev/view_predict.ts` prints the expected rows), `stair-noon-plain` (air transmittance ~0.64 at 5 km, ~0.26 at 16.5 km), `apadana-hall-in`, a day with cumulus, `rain-approach`.
2. (Fixed by the lead at merge) exposure and the probes' eye test now multiply the sun by the horizon visibility (`sky.eyeSunVisibility`, `EYE_SKY`).
3. **Thin cloud is far too bright from the sunward side**: layers of optical depth 0.5–5 reflect 0.55–0.90 where a two-stream model gives 0.04–0.27 (Q-253). Thick cloud calibrated (albedo 0.75, base 0.25).
- Smoke and town haze ignore the horizon (`fire.ts`, `settlement/haze.ts`; hook `EYE_SKY.sunVisibilityAt`). Rain-shaft contrast under the new air unmeasured. Shade under a clear sky is bluer (skylight colour now the physical sky's, blue/red 1.9, was 1.2; brightness unchanged, Q-255). At `quality=test` the day sky has no clouds. Horizon map error up to 6° where terrain within ~100 m sets the skyline (Q-256). GPU cost unmeasured: each lit fragment gains 3 textures and ~11 fetches; people and near trees use ~10 of 16 texture slots; the CPU re-bakes the sun atlas every 0.1° of azimuth (5–25 ms each).

**Changed:** terrain horizon map `public/generated/horizon_map.{json,dat}` (2.9 MB, `tools/build_horizon.ts`): 3 levels (40, 256, 1,120 m), 36 azimuths; vs independent DEM ray casts mean 0.11–0.19°, max 1.43° (`bench-reports/horizon-map-accuracy.txt`); applied through the sun's and moon's `colorNode` (reaches meshes with receiveShadow = false; tested). **True first sun on the Grand Stair landing, day 0: upper limb 06:25, centre 06:26 (DEM); map 06:27; Apadana court 06:32; at 05:51 the mountain's shadow reaches 9.75 km W.** Aerial perspective `src/sky/aerial.ts` replaces FogExp2: visibility 43 km clear, 7 km dust, 1.4 km mist; a clear spring day veils 60 % at 10 km (old fog 3–4 %); haze colour from the calibrated sky, its direct-sun part follows the horizon; clouds in the same air. Physical sky by day (within 20–30 % of CIE type 12 away from the sun; Preetham was ~2× too dark); haze changes rebuild the sky at 3 ms/frame. Clouds: 3 multiple-scattering octaves plus a two-stream term, ambient from sky and ground, march from the cloud base; cover table rebuilt; sunlit cumulus 3.2× the sunlit ground (was 2.0×). Light shafts `src/render/airlight.ts`: half resolution, 16 steps over 48 m against the nearest two cascades, dust 2e-4 /m, forward scattering, inside the probe volumes only.
**Verification:** tsc clean; full vitest --maxWorkers=2 on its branch: 434 passed, 1 skipped; lint_chrono OK; no renders.

## Faces, hair and cloth (D-155) — merged; no browser render after the baseline
**Still reads as CG**
1. **No browser check after the baseline** (`shots/agent-faces/before/`): checked only by node tests, a node build of the material to WGSL and GLSL, and a CPU copy of the material (node previews, no TRAA/SSGI). Next browser run: eyes and lashes, beard edges, the bob, the headcloth, the kandys, the seated skirt, the stress-view counts.
2. **Hair still reads as a textured cap**: shells with a ragged alpha edge, not strands or cards; the long beard is a squared box with drawn wavy locks (the relief convention).
3. **Skin, brows and garments are generated, not captured**: one procedural skin map for everyone (only brows and blotches vary); brows painted into the skin map; no iris refraction; garments are body shells, most folds shading only. Also: kandys sleeves are flattened tubes; the fluted hat is a lathe; hands and teeth untouched; MakeHuman lids look heavy; GPU fragment cost unmeasured; its full vitest run was stopped at the deadline.
**Changed (C unless noted):** skin wrap lighting by channel and curvature (nose 81 /m, forehead 8, ears ≤ 250), thin-part transmission, two specular lobes with an oil map, a 2048×1024 skin atlas (colour; creases, oil, age lines, translucency), pores and mottling band-limited (gone below ~3 px); tone by origin (Q-240); brows vary per person (brow/forehead 0.22–0.44 across 16 people). Eyes: sclera 0.64 (was 0.30), procedural iris with limbal ring, iris colour by origin (Q-244), analytic cornea highlight following the gaze. Lashes: 72 tapering clumps per upper lid. Court curls in jittered rows (relief convention B), natural curls for workers, straight strands for the bob, highlights along strands; long-beard lock autocorrelation 0.997 → 0.52. Bob: a curtain from the head's widest part to the jaw, 12 mm → 2–5 mm. Cloth: weave bump by fibre, sheen lobe, shaded pleats (Q-247), sleeve folds. Kandys fronts, folds, border, empty sleeves outside the coat (Q-242). Felt cap full detail (Q-243). Headcloth without torn edges (1,473 → 1,215 triangles; Q-249). Seated skirt drops 3.6 cm between the knees. Grime by trade (Q-248). Bugs fixed: a comment swallowing the grime's material mask (dust on eyes/hair/metal), a reversed smoothstep, look flags rounded before decoding.
**Budgets** (`tools/dev/human_budget.ts`): stress view 2–20 m 2,621,354 → 2,608,117 triangles, 15 draws; shadow 245,400 → 244,938 per map; vertex source 17.61 → 17.14 MB; **skin texture 5.59 → 11.18 MB** (the one increase). New tools: `tools/dev/human_cpu.ts`, `face_preview.ts`, `human_budget.ts`; tests `humans_faces.test.ts`, `humans_shader.test.ts`.

## Simulation, round-4 fixes (D-150) — merged; **the final commit is not soak-verified**
- The last full `npm run soak` ran on `8448975`: 7 of 8 gates pass; `plansWellFormed` failed with 15 issues (1 teleport: toddler 16679 day 114 following his father to the grain-heap watch; 13 "alone": the watch left a widower's 9-year-old alone at night, households 9844, 9805, and the children of 4100; 1 "apart": 6423, a one-year-old with a wet nurse, day 294). The final commit `42288b2` fixes all three (no night watch that would leave children under ten without a grown woman at home; a child's few seconds between two walks is a walk of its own), checked only on the failing days (87, 90, 96, 114, 294): 0 issues. **Run `npm run soak` first thing.**
- Tests on the final commit: the new `tests/people_days_r5.test.ts` 18/18; the full suite last ran on `cb3e96d` (423 passed, 1 skipped); `people.test.ts` timed out once under load and passed alone.
- Per finding: S1 fields fraction split by sex (men 0.9/0.9/0.8/0.3 by season), men's other work by season (`lives.json` `farm_men_other_work`, each option sourced), daylight rest at home capped 2.5 h (3.5 h winter): 0 % of able farming men idle outside winter (31224 on day 21 now works). S2 the grain-heap watcher sleeps at the floor by turns. S3 the leader's round visits his own file's posts from `P.rota(d)`; the eve of the year has a rota. S4 herding bands as families in tents (E-49's sources: the Qashqai analogue, HDT 1.125): 39 bands, 908 people, 0 plan issues, 0 near-copy days. S5 infants sleep between feeds (median 15.8 h at 0–3 months). S6 the child on the threshing floor carries a stick. S7 women's winter days: 3.6–3.9 h spinning and weaving. S10 out-and-straight-back walks 6,630 → 2, back-to-back walks 12,840 → 576. S12 `tools/shadow_days.ts` stratified sample, fresh simulation per detailed agent, days 0–353, full log. S8/S9 logged (Q-221, Q-222). Q-220–Q-226.
- Soak numbers on `8448975`: worst detailed-agent variety 0.016; population variety worst 0.094 (44754, unchanged since round 2); events 14–20 kinds a week; 15,454,999 person-days; 42 min.
- **Round-5 input:** `REVIEWS/shadow_days_input_seed1_pick97.txt` (final code, not scored): 20 living, present, distinct people: 6 detailed agents (3 guards) and 14 of the population (2 Terrace workers, 5 townspeople, 7 others).
